import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["assets.aceternity.com", "i.ibb.co"], // Add the required hostname
  },
};

export default nextConfig;
